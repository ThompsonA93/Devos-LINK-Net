#!/bin/bash
# One of the dependencies for Chainlink
# Based on Ubuntu 22.04
sudo apt install nodejs
sudo apt install npm
sudo npm install -g n
sudo n 16   # Version 16, correct for whichever version is required